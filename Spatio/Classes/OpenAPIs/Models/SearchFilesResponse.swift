//
// SearchFilesResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** In-memory substring search across the caller&#39;s files. Provider filtering isn&#39;t standardized across providers, so the platform lists up to ~500 files and filters locally — &#x60;total&#x60; is the pre-truncation count, not the global count.  */
public struct SearchFilesResponse: Codable, JSONEncodable, Hashable {

    public var files: [SpatioFile]?
    public var total: Int
    public var hasMore: Bool
    public var query: String

    public init(files: [SpatioFile]? = nil, total: Int, hasMore: Bool, query: String) {
        self.files = files
        self.total = total
        self.hasMore = hasMore
        self.query = query
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case files
        case total
        case hasMore
        case query
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(files, forKey: .files)
        try container.encode(total, forKey: .total)
        try container.encode(hasMore, forKey: .hasMore)
        try container.encode(query, forKey: .query)
    }
}

